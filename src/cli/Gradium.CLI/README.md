# Gradium.CLI

Command-line interface for the Gradium SDK generated with AutoSDK.

## Installation

```bash
dotnet tool install --global Gradium.CLI --prerelease
```

## Usage

```bash
gradium --help
gradium api --help
```