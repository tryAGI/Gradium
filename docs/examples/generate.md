# Generate

Basic example showing how to create a client and make a request.

This example assumes `using Gradium;` is in scope and `apiKey` contains your Gradium API key.

```csharp
using var client = new GradiumClient(apiKey);
```